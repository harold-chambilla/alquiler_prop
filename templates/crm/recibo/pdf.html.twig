<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recibo de Consumo</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f9;
            color: #333;
        }
        .recibo-header {
            text-align: center;
            margin-bottom: 20px;
        }
        .recibo-header h1 {
            margin: 0;
            font-size: 24px;
            color: #4CAF50;
        }
        .recibo-header p {
            margin: 5px 0;
        }
        .recibo-details {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        .recibo-details th, .recibo-details td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        .recibo-details th {
            background-color: #f4f4f9;
            color: #4CAF50;
            font-weight: bold;
        }
        .recibo-details tbody tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .recibo-total {
            font-size: 18px;
            font-weight: bold;
            text-align: right;
            margin-top: 20px;
        }
        .recibo-footer {
            margin-top: 30px;
            text-align: center;
            color: #777;
        }
        .footer-info {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
        }
        .footer-info div {
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="recibo-header">
        <h1>Recibo de Consumo</h1>
        <p><strong>Recibo N°:</strong> {{ recibo.id }}</p>
        <p><strong>Fecha:</strong> {{ recibo.ReFechaEmision|date("d/m/Y") }}</p>
    </div>

    <table class="recibo-details">
        <thead>
            <tr>
                <th>Concepto</th>
                <th>Consumo (kWh)</th>
                <th>Precio (S/.)</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>Luz de Cuarto</td>
                <td>{{ consumoLuz ? consumoLuz.getLelDato() : 'N/A' }} kWh</td>
                <td>S/. {{ consumoLuz ? (consumoLuz.getLelDato() * 1)|number_format(2, '.', ',') : 'N/A' }}</td>
            </tr>
            <tr>
                <td>Luz de Escalera</td>
                <td>{{ consumoEscalera ? consumoEscalera.getLelDato() : 'N/A' }} kWh</td>
                <td>S/. {{ consumoEscalera ? (consumoEscalera.getLelDato() * 1)|number_format(2, '.', ',') : 'N/A' }}</td>
            </tr>
        </tbody>
    </table>

    <p class="recibo-total">
        <strong>Total a pagar:</strong> S/. {{ recibo.RePagoTotal|number_format(2, '.', ',') }}
    </p>

    <div class="footer-info">
        <div><strong>Alquiler Mensual:</strong> S/. {{ contrato.CoAlquilerMensual|number_format(2, '.', ',') }}</div>
        <div><strong>Agua:</strong> S/. {{ contrato.CoAgua|number_format(2, '.', ',') }}</div>
    </div>

    <div class="recibo-footer">
        <p>Gracias por su pago puntual.</p>
        <p><em>Por favor conserve este recibo como comprobante.</em></p>
    </div>
</body>
</html>

